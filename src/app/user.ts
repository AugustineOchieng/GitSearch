export class User {
  constructor(public username: string, public html_url: any, public avatar_url:string, public public_repos: any, public repo_url: any, public bio: any, public created_at: Date,public following:number, public followers:number, public location:string, public login:string,public name:string,hireable:string) {}
}
